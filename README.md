# ansible-role-coreos_ansible_install

## Installation

Using `git`:

```shell
$ git clone https://github.com/Cougar/ansible-role-coreos_ansible_install coreos_ansible_install
```

## Usage

Look at example playbook `example_playbook.yml`
